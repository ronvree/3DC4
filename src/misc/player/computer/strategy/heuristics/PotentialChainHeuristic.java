package misc.player.computer.strategy.heuristics;

import misc.Color;
import misc.GameState;

/**
 * Scores a grid based on how many chains can still be formed
 */
public class PotentialChainHeuristic implements Heuristic {

    @Override
    public int score(GameState state, Color color) {
        return 0; // TODO
    }

}
